apiVersion: v1
kind: Service
metadata:
  name: {{ include "nexd.fullname" . }}
  labels:
    {{- include "nexd.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nexd.selectorLabels" . | nindent 4 }}
